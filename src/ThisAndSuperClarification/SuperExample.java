package ThisAndSuperClarification;

//Represent the parent class "Variable & Method "
//if we invoke constructor exist in parent class when we exist in inherited class use super(),if method use super.methodname(,);

public class SuperExample {
    public static void main(String[] args) {
        Dog D=new Dog();
        D.Display();


    }
}
