package Java_Sessions;
//all method inside interface by default abstract
//all variable inside interface by default final =constant
//if there is a method we didn`t do an implementation inside implementation class we must do it as abstract
public interface Carinterface {
    public void Motor();
    public  void DisplayScreen();
    int Var=10; // el variable keda final static constant
    //we can`t take an object from interface
}
